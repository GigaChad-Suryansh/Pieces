# Engine Atlas

Interactive educational engine explorer for everyday cars and motorcycles.

## Run
npm install
npm run dev

The current prototype uses procedural SVG for an immediately runnable model. Production can replace it with GLTF/GLB assemblies where every mechanical component is a separate selectable node.